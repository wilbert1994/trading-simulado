{
  "rules": {
    ".read": true,
    ".write": true,
    "portfolio": {
      ".validate": "newData.hasChildren(['balance', 'initialBalance', 'totalPnl', 'totalPnlPercent', 'openPositions', 'lastUpdated'])"
    }
  }
}
